services:
  node-agent:
    build: ../services/node-agent
    env_file:
      - ./env.node.example
    volumes:
      - node_data:/data
      - ../scripts:/opt/mini-ogas/scripts:ro
    restart: unless-stopped

volumes:
  node_data:
