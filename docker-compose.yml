# Mini-OGAS root Compose stack: central services and three SimPy node agents.
# Required secrets are read from the shell or a root .env file.

include:
  - deploy/docker-compose.central.yml
  - deploy/docker-compose.node.yml
