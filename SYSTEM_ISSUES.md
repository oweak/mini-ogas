# Mini-OGAS 系统问题报告

报告时间：2026-06-02
报告范围：分布式服务启动、自检真实性、AI 调用链路

---

## 1. 根本性问题：无服务编排机制

**现状：** 系统由 9 个独立进程组成（4 个 HTTP 服务 + 5 个节点代理），但没有统一启停脚本。每次启动需要手动在正确目录执行命令。

**影响：**
- 重启电脑后无法一键恢复系统
- 当前会话中，8081/8082/8083 端口由上次会话残留进程占用，掩盖了启动脚本不存在的事实
- 开发/演示时浪费时间在手动管理进程上

**解决方案：** 编写 `scripts/start-all.ps1`（Windows PowerShell），执行：
1. 强制终止旧的 central-api / microservice / agent 进程
2. 按依赖顺序启动所有服务（central-api → microservices → agents）
3. 每启动一个即调用 `/health` 验证
4. 全部成功后打印访问地址

---

## 2. 自检流程中的欺骗性行为

**现状：** preflight 检查微服务连通性时，仅调用 `GET /health`。如果端口被旧进程占用（即使进程属于另一个终端会话），仍显示"通过"。这给人系统正常的错觉，实际上新进程根本没启动成功。

**应该改为：**
- 启动脚本本身负责验证——启动失败则明确报错退出
- preflight 检查应增加进程新鲜度验证（如检查进程启动时间是否早于本次 systemctl/launcher 启动时间）
- 不再用"端口有响应"等同于"服务已正确启动"

---

## 3. AI 调用依赖外部 API Key

**现状：** DeepSeek API Key 通过环境变量传入。如果 Key 缺失或失效：
- AI 诊断回退到 `local-fallback`（硬编码规则，非真正 AI）
- 虚拟试运行回退到本地规则
- 系统不会尝试其他免费模型或本地模型

**风险：**
- DeepSeek 服务不可用时，所有 AI 功能变为静态规则引擎
- 用户未配置 API Key 时，系统不会主动提示替代方案

**可选替代方案（需用户确认）：**
| 方案 | 优点 | 缺点 |
|------|------|------|
| DeepSeek（当前） | 中文理解好，便宜 | 需要 API Key |
| Ollama + Qwen 本地模型 | 完全离线，无费用 | 需 GPU 内存，响应慢 |
| LM Studio + 本地模型 | GUI 管理方便 | Windows only |
| Groq Cloud (免费 tier) | 免费，速度快 | 需外网，速率限制 |
| 纯规则引擎 | 无依赖，稳定 | 非真正 AI，诊断质量有限 |

---

## 4. 节点代理的指标真实性

**当前状态（已修复）：** 5 个 node-agent 进程使用 psutil 采集真实 CPU/内存/磁盘数据。

**遗留问题：**
- 代理进程无守护/自动重启机制——崩溃后该节点静默离线
- 所有代理共用同一主机的 psutil 数据（因为都跑在一台机器上），车间之间的 CPU/内存差异仅靠随机偏移模拟
- 真正的分布式需要每个代理跑在独立物理机/虚拟机上

---

## 5. 前端对节点状态的误解

**现象：** 仪表盘显示"子节点心跳 0/3 在线"，但 API 返回 5 个节点全部在线。

**原因：** 前端 `mergeAlarmFetchResults` 只统计特定类型节点，过滤逻辑与后端的 5 节点注册不一致。这是前端代码层面的 display bug，不影响实际数据。

---

## 6. VirtualBox 检测显示"未检测到"

**现状：** 节点代理是原生 Python 进程，不是 VirtualBox 虚拟机。

**说明：** 这不是 bug——当前架构设计就是原生进程模拟分布式。如果项目定位需要展示虚拟机管理能力，需要用 Vagrant 或手动创建 VirtualBox VM 并部署 agent。如果不需要，前端应移除 VirtualBox 检测面板。

---

## 总结

| 问题 | 严重程度 | 状态 |
|------|----------|------|
| 无服务编排/启停脚本 | **严重** | 本次修复 |
| 自检不验证进程新鲜度 | **严重** | 待解决 |
| AI 依赖单一外部 API | 中等 | 待用户选择方案 |
| node-agent 无守护机制 | 中等 | 待实施 |
| 前端节点计数 bug | 低 | 待修复 |
| VirtualBox 面板无意义 | 低 | 待决定 |
